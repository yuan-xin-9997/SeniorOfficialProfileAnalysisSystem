# 开发提示词（Python+Fastapi+Vue版本）

## 初始

我希望构建一个中国（不仅限于中国，目前只考虑中国）高级官员的履历分析系统，高级官员特指中国共产党中央委员会的委员和候补委员。

这个系统应该有4个模块，分别是：

1. 数据抓取模块：定期从网络上更新已经存储的高级官员的履历信息
2. 数据存储模块：存储每个高级官员的履历，履历包含出生年月、出身地，从小到大逐年的经历，如果是上大学则存储上的大学名字专业，如果是从政则存储从政地点、职位、秘书是谁、同僚有哪些等等
3. 数据分析模块：比如可以根据给定需要分析高级官员之间是否为同盟
4. 数据展示模块：可视化分析，高级官员之间的联系

以上是我的初步构想，请基于我的构想写一个软件需求规格说明书，并将软件规格说明书保存在当前目录，如果有不清楚的请停下来向我提问。

以下是我对待确认问题的回答，请基于我的回答生成系统设计说明书，并保存。

以下问题不会阻塞一期原型设计，但建议在进入详细设计前确认：

1. 初始覆盖范围是第几届中央委员会，还是从最近一届开始？从最近一届的开始
2. 系统是否仅供个人或内部研究使用，是否需要公网访问？个人内部研究使用
3. 是否需要支持多用户协作和严格权限审批？区分管理员权限和普通用户权限
4. 是否允许使用大语言模型辅助解析履历文本？允许
5. 关系分析权重应由研究人员配置，还是先采用系统默认权重？由研究人员配置，但是系统设置初始默认权重
6. 是否需要支持英文界面或其他国家政治人物扩展？暂不需要
7. 是否需要图数据库作为一期必选组件，还是先用关系型数据库实现关系边表？你自己决定
8. 数据导出是否需要水印、访问控制或脱敏策略？不需要
9. 对“秘书是谁”等敏感关系，是否默认隐藏，仅在有高可信公开来源时展示？不需要
10. 抓取频率是每日、每周，还是按数据源差异化配置？默认按周，需要可配置

## 软件迭代提示词 2026-6-21

之前实现的是用k8s实现的，我现在想用最简单的方式实现，fastapi+vue的方式。然后参考E:\Sync2NAS\坚果云同步\ObsidianVault\资讯\特朗普真实社交\JenkinsConfig下也写一个Jenkinsfile。

另外需要写好为Windows和Linux的启停、status脚本。
将实现方式、部署、运维方式写到README.md中。

部署需求：

1. 端口、路径都必须可配置，不能写死在代码中
2. 日志要按天分割，分割后的日志依然以.log结尾
3. 确保.gitignore文件中将日志文件排除了
