# 中国高级官员履历分析系统

- `src_gpt5.5/`：当前推荐版本，FastAPI + Vue + SQLite，支持本地直接运行和 Jenkins 部署。
- `src_gpt5.5_k8s/`：早期 Kubernetes/minikube 版本，保留用于对照。

新版使用说明见 `src_gpt5.5/README.md`，Jenkins 流水线位于 `src_gpt5.5/JenkinsConfig/Jenkinsfile`。
